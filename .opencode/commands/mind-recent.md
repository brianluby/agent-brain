---
description: Show recent Agent Brain memories
---

Use the `mind` tool with:

- `mode`: `"recent"`
- `limit`: `20`

If `$ARGUMENTS` is a positive integer, use that value for `limit` instead.

Then list recent memories in reverse chronological order.
