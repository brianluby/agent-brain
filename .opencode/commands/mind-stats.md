---
description: Show Agent Brain memory statistics
---

Use the `mind` tool with:

- `mode`: `"stats"`

Then summarize the key metrics (total observations, sessions, oldest/newest memory, and top types).
